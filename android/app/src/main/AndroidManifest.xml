<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Bluetooth permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Internet for Strava API -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Foreground service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Alarm permissions for periodic connection attempts -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Wake lock to keep device awake during periodic connection attempts -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Request battery optimization exemption -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WalkolutionOdometer"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.WalkolutionOdometer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="Settings"
            android:theme="@style/Theme.WalkolutionOdometer" />

        <!-- Set Lifetime Totals Activity -->
        <activity
            android:name=".SetLifetimeTotalsActivity"
            android:exported="false"
            android:label="Set Lifetime Totals"
            android:theme="@style/Theme.WalkolutionOdometer" />

        <!-- Strava Settings Activity -->
        <activity
            android:name=".StravaSettingsActivity"
            android:exported="true"
            android:label="Strava Settings"
            android:launchMode="singleTask"
            android:theme="@style/Theme.WalkolutionOdometer">
            <!-- Strava OAuth callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="walkolution"
                    android:host="walkolution" />
            </intent-filter>
        </activity>

        <!-- Logs Activity -->
        <activity
            android:name=".LogsActivity"
            android:exported="false"
            android:label="Device Logs"
            android:theme="@style/Theme.WalkolutionOdometer" />

        <!-- BLE Background Service -->
        <service
            android:name=".BleService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Periodic Connection Receiver -->
        <receiver
            android:name=".PeriodicConnectionReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.mypeople.walkolutionodometer.PERIODIC_CONNECTION_ATTEMPT" />
            </intent-filter>
        </receiver>
    </application>

</manifest>